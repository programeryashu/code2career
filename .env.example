# mock = in-memory demo data, live = fetch from the FastAPI backend
VITE_API_MODE=mock

# Only used when VITE_API_MODE=live.
# Local backend (proxied by Vite): leave unset, /api is proxied to 127.0.0.1:8000
# Production: set to the Render URL, e.g. https://skillswap-api.onrender.com
VITE_API_URL=
